using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AddPrefix : MonoBehaviour {

	// Use this for initialization
    public GameObject inputText;
    string upText;
	void Start () {
		
	}
	
	// Update is called once per frame
    void Update()
    {
        //upText = inputText.GetComponent<Text>().text;
        //if (upText.Length > 2)
        //{
        //    this.GetComponent<Text>().text = upText[0].ToString() + upText[1].ToString() + this.GetComponent<Text>().text;
        //}
    }
}
